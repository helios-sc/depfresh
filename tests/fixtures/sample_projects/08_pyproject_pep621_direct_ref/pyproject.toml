[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-pep621-direct-ref"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "click>=8.3.1",
    "demo @ https://example.com/demo-1.0.tar.gz",
]
