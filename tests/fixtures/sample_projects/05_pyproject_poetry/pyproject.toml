[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "test-poetry"
version = "0.1.0"
description = "Test project for depfresh poetry format"

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.3.1"
packaging = "^23.2"
